# NSSA244 Lab 1 – ARM Template Export
### Overview 
This ARM template deploys a Linux virtual machine named WEB1 along with its required networking resources. It includes a virtual network with subnets, a network security group allowing SSH and HTTP, a public IP address, a network interface, and attached managed disks. The template was exported from the RG-LAB1 resource group using the Azure Portal.

Export Date: January 13, 2026